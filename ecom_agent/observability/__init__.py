"""可观测性：把一次 run 落成可回放的产物（run.json / steps.jsonl / 截图 / 报告）。

为什么单独成包而不是塞进 runtime：
  这一层是**纯数据变换 + 文件 IO**，不碰浏览器、不碰 LLM、不碰 asyncio 的调度。
  分开之后它可以用 `tmp_path` 直接测，不需要起浏览器 —— 而"可回放"这件事
  恰恰是最需要被穷举测试的（报告少一列、截图错帧、脱敏漏一处，都只有测试能发现）。
"""
